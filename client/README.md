# Marketplace CRM — Frontend

React + Vite + TypeScript + Tailwind. Consome a API em `../server`.

```bash
npm install
npm run dev   # http://localhost:5173, com proxy de /api para http://localhost:4000
```

Veja a documentação completa do projeto em [`../MARKETPLACE-CRM.md`](../MARKETPLACE-CRM.md).
